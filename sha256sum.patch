- Calculate the sha256sum for every file recursivelay and save it to sha256sums in PowerShell:

`Get-ChildItem -Recurse -File | ForEach-Object { sha256sum $(Resolve-Path -Relative $_.FullName) >> sha256sums }`
